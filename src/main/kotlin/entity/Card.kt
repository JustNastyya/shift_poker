package entity

/** class represents a single card with its value and suit
 * @param value of type `CardsValue`
 * @param suit of type `CardSuit`
 * */
data class Card(val value: CardValue, val suit: CardSuit)
